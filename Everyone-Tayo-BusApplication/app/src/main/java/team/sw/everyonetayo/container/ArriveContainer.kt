package team.sw.everyonetayo.container

/*
import team.sw.everyonetayo.controller.login.LoginController
import team.sw.everyonetayo.model.arrive.ArriveNoticeService
import team.sw.everyonetayo.model.login.LoginService
import team.sw.everyonetayo.repository.login.MemoryLoginRepository

class ArriveContainer {

    companion object {
        val instance: ArriveContainer = ArriveContainer();
    }

    private var arriveNoticeService: ArriveNoticeService? = null

    fun arriveNoticeService():ArriveNoticeService{
        if(arriveNoticeService==null){
            arriveNoticeService = ArriveNoticeService()
            return arriveNoticeService!!
        }else{
            return arriveNoticeService!!
        }
    }
}
 */