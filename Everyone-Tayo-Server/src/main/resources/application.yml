spring:
  datasource:
    url: jdbc:mariadb://localhost:3306/everyontayo
    username: root
    password: admin
    driver-class-name: org.mariadb.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        format_sql: true
